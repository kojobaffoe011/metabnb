import React from "react";
import { Modal } from "./modal";

const SideMeNuModal = (props) => {
  return (
    <Modal {...props}>
      <div style={{ width: "100px", height: "100vh", borderRadius: "0px" }}>
        a
      </div>
    </Modal>
  );
};

export default SideMeNuModal;
